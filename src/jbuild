(library
 ((name hardcaml_waveterm)
  (public_name hardcaml_waveterm)
  (libraries (base
              hardcaml
              num
              re
              stdio))
  (preprocess (pps (ppx_jane ppxlib.runner)))))


(jbuild_version 1)
